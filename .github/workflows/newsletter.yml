name: EM Weekly Digest

on:
  schedule:
    - cron: '0 7 * * 1'  # ogni lunedì alle 7:00 UTC (= 8:00 ora italiana invernale, 9:00 ora legale)
  workflow_dispatch:       # permette lancio manuale

jobs:
  invia-newsletter:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Installa Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installa dipendenze
        run: |
          pip install google-auth-oauthlib google-auth-httplib2 google-api-python-client

      - name: Verifica file presenti
        run: ls -la

      - name: Aggiorna credenziali in config.py
        run: |
          sed -i "s|ANTHROPIC_API_KEY  = \".*\"|ANTHROPIC_API_KEY  = \"${{ secrets.ANTHROPIC_API_KEY }}\"|" config.py
          sed -i "s|GMAIL_USER         = \".*\"|GMAIL_USER         = \"${{ secrets.GMAIL_USER }}\"|" config.py

      - name: Esegui newsletter
        run: python newsletter_rss.py
